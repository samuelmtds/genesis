<?xml version="1.0" encoding="ISO-8859-1"?>
<document>
  <properties>
    <title>genesis' Downloads</title>
    <author email="mister__m@dev.java.net">Michael Nascimento Santos</author>
  </properties>
  <body>
    <section name="genesis' Downloads">
      <p>There are several distribution bundles available for downloading.
      They are:</p>
      <ul>
        <li><b>genesis-dist-{version}.tar.gz</b> and
        <b>genesis-dist-{version}.zip</b>: genesis distribution jars and the 
        annotation.properties file.</li>
        <li><b>genesis-dependencies-{version}.tar.gz</b> and
        <b>genesis-dependencies-{version}.zip</b>: all the jar files for genesis 
        dependencies, such as AspectWerkz, Hibernate, Jakarta Commons and 
        Thinlet.</li>
        <li><b>genesis-empty-project-{version}.tar.gz</b> and
        <b>genesis-empty-project-{version}.zip</b>: comes with an empty project
        structure so it can be used to start a new genesis-based project.</li>
        <li><b>genesis-useradmin-sample-{version}.tar.gz</b> and
        <b>genesis-useradmin-sample-{version}.zip</b>: comes with the
        <code>useradmin</code> genesis sample application.</li>
        <li><b>genesis-src-{version}.tar.gz</b> and
        <b>genesis-src-{version}.zip</b>: comes with genesis source code only 
        (no additional jar files).</li>
        <li><b>genesis-docs-{version}.tar.gz</b> and
        <b>genesis-docs-{version}.zip</b>: only genesis documentation.</li>
        <li><b>genesis-xdoclet-extensions.tar.gz</b> and
        <b>genesis-xdoclet-extensions.zip</b>: modified xdoclet distribution
        files with additional tasks.</li>
        <li><b>genesis-all-{version}.tar.gz</b> and
        <b>genesis-all-{version}.zip</b>: comes with all binary files, samples,
        source code and documentation mentioned above.</li>
      </ul>
      <p>All distribution files for this release can be found
      <a href="https://genesis.dev.java.net/servlets/ProjectDocumentList?folderID=2698&amp;expandFolder=2698&amp;folderID=0">here</a>.</p>
    </section>
  </body>
</document>