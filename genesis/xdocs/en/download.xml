<?xml version="1.0" encoding="ISO-8859-1"?>
<document>
  <properties>
    <title>genesis' Downloads</title>
    <author email="mister__m@dev.java.net">Michael Nascimento Santos</author>
  </properties>
  <body>
    <section name="genesis' Downloads">
      <p>There are several distribution bundles available for downloading.
      They are:</p>
      <ul>
        <li><b>genesis-docs-{version}.tar.gz</b> and
        <b>genesis-docs-{version}.zip</b>: genesis documentation.</li>
        <li><b>genesis-bin-{version}.tar.gz</b> and
        <b>genesis-bin-{version}.zip</b>: genesis binary distribution.
        This archive contains genesis jars and an empty project which can be
        used to start a new genesis application. This archive does not contain
        any jars genesis depends on, such as Hibernate.</li>
        <li><b>genesis-full-bin-{version}.tar.gz</b> and
        <b>genesis-full-bin-{version}.zip</b>: genesis full binary distribution.
        This archive contains the following files: all files contained in
        <code>genesis-bin</code>, including the empty project, and all the
        necessary jar files for genesis dependencies, such as AspectWerkz,
        Hibernate, Jakarta Commons and Thinlet.</li>
        <li><b>genesis-full-bin-plus-docs-{version}.tar.gz</b> and
        <b>genesis-full-bin-plus-docs-{version}.zip</b>: genesis full binary
        distribution and documentation. This archive contais all jars contained
        in <code>genesis-full-bin</code>, the empty project structure and
        complete genesis documentation, as well as some additional genesis
        samples, such as a simple calculator (SimpleCalculator)
        and a user administration application (Useradmin).</li>
        <li><b>genesis-useradmin-sample-{version}.tar.gz</b> and
        <b>genesis-useradmin-sample-{version}.zip</b>: contain both source code
        as well as binary files for the UserAdmin sample.</li>
        <li><b>genesis-src-only-{version}.tar.gz</b> and
        <b>genesis-src-only-{version}.zip</b>: genesis source code only, with
        no jars.</li>
        <li><b>genesis-src-{version}.tar.gz</b> and
        <b>genesis-src-{version}.zip</b>: the same as
        <code>genesis-src-only</code> plus <code>genesis-full-bin</code>.</li>
        <li><b>genesis-src-plus-docs-{version}.tar.gz</b> e
        <b>genesis-src-plus-docs-{version}.zip</b>: the same as
        <code>genesis-src</code> plus genesis complete documentation.</li>
        <li><b>modified-xdoclet-1.2.zip</b>: modified xdoclet 1.2 distribution
        files with additional tasks.</li>
      </ul>
      <p>All distribution files for this release can be found
      <a href="https://genesis.dev.java.net/servlets/ProjectDocumentList?folderID=2644&amp;expandFolder=2644&amp;folderID=0">here</a>.</p>
    </section>
  </body>
</document>